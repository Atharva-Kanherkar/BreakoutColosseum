import SignupSuccess from '@/components/SignupSuccess'; // Adjust path if necessary

export default function SignUpConfirmationPage() {
  return <SignupSuccess />;
}

// Optional: Add metadata
export const metadata = {
  title: 'Signup Successful - ChainArena',
  description: 'Your ChainArena account has been created successfully.',
};